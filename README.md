# Storybook Vision Platform

Storybook для Vision Platform с Design System, компонентами и документацией.

## 📚 Структура

```
storybook_repo/
├── .storybook/          # Конфигурация Storybook
│   ├── main.mjs        # Основная конфигурация
│   ├── preview.jsx     # Настройки preview
│   └── ...
├── src/
│   ├── stories/        # Все stories и документация
│   │   ├── Начни тут/  # Главная страница
│   │   ├── Настоящее/  # Текущие компоненты
│   │   ├── Будущее/    # Новые компоненты и Design System
│   │   └── Устаревшее/ # Устаревшие компоненты
│   └── design-tokens/  # Дизайн-токены
│       ├── design-tokens.json
│       └── tokenParser.ts
└── package.json
```

## 🚀 Быстрый старт

### Установка

```bash
npm install
# или
yarn install
```

### Запуск Storybook

```bash
npm run storybook
# или
yarn storybook
```

Storybook будет доступен по адресу: http://localhost:6006

### Сборка Storybook

```bash
npm run build-storybook
# или
yarn build-storybook
```

## 📖 Структура Storybook

### Начни тут

Главная страница с описанием структуры Storybook и навигацией по разделам.

### Настоящее

Компоненты, которые используются в проекте сейчас. Все рабочие компоненты из Legacy UIKit.

### Будущее

Новые компоненты и системы:
- **Design System** — идеальная система дизайн-токенов
  - All Tokens — полный справочник всех токенов
  - Foundations — детальные страницы по каждому типу токенов
  - Layout — паттерны компоновки
  - Examples — примеры применения
- **Grafana UI Fork** — полный форк компонентов из Grafana UI

### Устаревшее

Компоненты, которые будут заменены. Архив для справки.

## 🎨 Design System

Система дизайн-токенов включает:

- **Spacing** — система отступов (примитивы и семантические)
- **BorderRadius** — система скруглений (примитивы и семантические)
- **Typography** — типографика (fontSize, fontWeight, lineHeight)
- **Colors** — цветовая система (примитивы и семантические)

Все токены доступны как CSS переменные и могут быть использованы в коде.

## 📝 Использование токенов

```tsx
// CSS переменные
<div style={{
  padding: 'var(--spacing-semantic-container-padding)',
  borderRadius: 'var(--radius-semantic-surface-default)',
  backgroundColor: 'var(--color-semantic-surface-interactive-default)',
}}>
  Контент
</div>
```

## 🔧 Конфигурация

Конфигурация Storybook находится в `.storybook/`:

- `main.mjs` — основная конфигурация (stories, addons, vite)
- `preview.jsx` — глобальные настройки (декораторы, параметры, темы)

## 📦 Зависимости

Основные зависимости для Storybook:
- `@storybook/react-vite` — интеграция Storybook с Vite
- `@storybook/addon-docs` — документация
- `@storybook/addon-a11y` — доступность
- `@grafana/ui` — компоненты Grafana UI

## 🔄 Процесс миграции

1. **Настоящее** → Компоненты используются сейчас
2. **Будущее** → Новые компоненты и системы готовятся к использованию
3. **Устаревшее** → Старые компоненты архивируются после замены

## 📖 Дополнительная информация

- Все stories находятся в `src/stories/`
- Design tokens находятся в `src/design-tokens/`
- Конфигурация Storybook в `.storybook/`

---

**Начните с раздела "Начни тут" в Storybook для навигации!**
