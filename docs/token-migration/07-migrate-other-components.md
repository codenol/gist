# Шаг 7: Миграция остальных компонентов

**Цель:** Обновить все остальные компоненты, чтобы они использовали новые токены.

## ⚠️ Важно

Мигрируйте компоненты по приоритету и по одному, проверяя результат после каждого.

## Что нужно сделать

### 1. Мигрировать Chip

**Действие:**
1. Откройте компонент Chip
2. Замените токены:
   - Старый: `Background.chips.default`
   - Новый: `color.chip.default.background.default`
3. Если есть состояния selected/disabled, замените их токены

**Результат:** Chip мигрирован

**Проверка:**
- ✅ Все токены заменены
- ✅ Визуально выглядит так же

---

### 2. Мигрировать Checkbox

**Действие:**
1. Откройте компонент Checkbox
2. Замените токены для всех состояний:
   - Default: `color.checkbox.default.background.default` и `color.checkbox.default.border.default`
   - Checked: `color.checkbox.checked.background.default` и `color.checkbox.checked.border.default`
   - Disabled: `color.checkbox.disabled.background.default`

**Результат:** Checkbox мигрирован

**Проверка:**
- ✅ Все состояния используют новые токены
- ✅ Все состояния работают корректно

---

### 3. Мигрировать Toggle

**Действие:**
1. Откройте компонент Toggle
2. Замените токены для всех состояний:
   - Default: `color.toggle.default.background.default`
   - Checked: `color.toggle.checked.background.default`
   - Disabled: `color.toggle.disabled.background.default`

**Результат:** Toggle мигрирован

**Проверка:**
- ✅ Все состояния используют новые токены

---

### 4. Мигрировать Message

**Действие:**
1. Откройте компонент Message
2. Для каждого варианта (info, error, warning, success) замените токены:
   - Background: `color.message.{role}.background.default`
   - Text: `color.message.{role}.text.default`

**Результат:** Message мигрирован

**Проверка:**
- ✅ Все варианты используют новые токены

---

### 5. Мигрировать Toast

**Действие:**
1. Откройте компонент Toast
2. Для каждого варианта замените токены:
   - Background: `color.toast.{role}.background.default`
   - Text: `color.toast.{role}.text.default`

**Результат:** Toast мигрирован

**Проверка:**
- ✅ Все варианты используют новые токены

---

### 6. Мигрировать Tooltip

**Действие:**
1. Откройте компонент Tooltip
2. Замените токены:
   - Background: `color.tooltip.default.background.default`
   - Text: `color.tooltip.default.text.default`

**Результат:** Tooltip мигрирован

**Проверка:**
- ✅ Все токены заменены

---

### 7. Мигрировать DataTable

**Действие:**
1. Откройте компонент DataTable
2. Замените токены:
   - Header background: `color.table.header.background.default`
   - Header text: `color.table.header.text.default`
   - Row background default: `color.table.row.background.default`
   - Row background hover: `color.table.row.background.hover`
   - Row background selected: `color.table.row.background.selected`

**Результат:** DataTable мигрирован

**Проверка:**
- ✅ Все части таблицы используют новые токены
- ✅ Все состояния работают корректно

---

### 8. Мигрировать Spinner

**Действие:**
1. Откройте компонент Spinner
2. Замените токен:
   - Fill: `color.spinner.default.fill.default`

**Результат:** Spinner мигрирован

**Проверка:**
- ✅ Токен заменен

---

### 9. Проверить все компоненты в макетах

**Действие:**
1. Найдите экземпляры всех мигрированных компонентов в макетах
2. Проверьте несколько экземпляров каждого компонента
3. Убедитесь, что они отображаются правильно

**Результат:** Все экземпляры работают корректно

**Проверка:**
- ✅ Все экземпляры отображаются правильно
- ✅ Нет визуальных изменений
- ✅ Нет ошибок

---

## ✅ Чеклист готовности

Перед переходом к следующему шагу убедитесь:

- [ ] Все компоненты мигрированы
- [ ] Все компоненты работают в обоих режимах (Light/Dark)
- [ ] Все состояния работают корректно
- [ ] Экземпляры в макетах работают правильно
- [ ] Нет визуальных изменений
- [ ] Нет ошибок в Figma

---

## 🎯 Следующий шаг

После миграции всех компонентов переходите к финальной проверке:

**→ [08-final-check.md](08-final-check.md)**

---

## 📝 Заметки

Запишите здесь свои наблюдения:

```
Мигрированные компоненты:
- Chip ✅
- Checkbox ✅
- Toggle ✅
- Message ✅
- Toast ✅
- Tooltip ✅
- DataTable ✅
- Spinner ✅

Проблемы, которые возникли:
- 
- 
```

