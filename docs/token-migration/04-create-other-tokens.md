# Шаг 4: Создание токенов для остальных компонентов

**Цель:** Создать новые токены для Chip, Checkbox, Toggle, Message, Toast, Tooltip, DataTable, Spinner.

## 📋 Компоненты для создания токенов

**Приоритет 3:**
- Chip
- Checkbox
- Toggle

**Приоритет 4:**
- Message
- Toast
- Tooltip

**Приоритет 5:**
- DataTable
- Spinner

## 📊 Таблицы соответствия

**ВАЖНО:** Перед созданием токенов обязательно изучите таблицы соответствия:

**→ [TOKEN_MAPPING_TABLES.md](TOKEN_MAPPING_TABLES.md)** - содержит подробные таблицы для всех компонентов с указанием:
- Откуда брать значения (старый токен или палитра)
- Рекомендации по цветам из палитры для недостающих токенов
- Обоснование выбора цветов

**Разделы в таблице:**
- [Chip](TOKEN_MAPPING_TABLES.md#chip)
- [DataTable](TOKEN_MAPPING_TABLES.md#datatable)
- Общие рекомендации по палитре

## Общий подход

Для каждого компонента:
1. Создать группу `color.{component}`
2. Создать подгруппы для вариантов/ролей
3. Создать токены для background, text, border (где нужно)
4. Создать для режимов Light и Dark
5. **Использовать таблицы соответствия для определения значений**

## Что нужно сделать

### 1. Chip

**📊 Используйте таблицу соответствия:** [TOKEN_MAPPING_TABLES.md#chip](TOKEN_MAPPING_TABLES.md#chip)

**Структура:**
```
color.chip.default.background.default
color.chip.selected.background.default
color.chip.disabled.background.default
color.chip.selected.text.default
color.chip.disabled.text.default
```

**Действие:**
1. Создайте группу `color.chip`
2. Создайте подгруппы: `default`, `selected`, `disabled`
3. В `default.background` создайте токен:
   - **Откуда брать:** Скопируйте значение из `Background.chips.default`
   - **Альтернатива:** Используйте `cool-gray/100` из палитры "Pallet"
4. В `selected.background` создайте токен:
   - **Откуда брать:** Если старого токена нет, используйте `bondi-blue/100` (Light) или `bondi-blue/800` (Dark) из палитры
   - **Обоснование:** Выбранный чип должен иметь акцентный цвет для визуального выделения
5. В `selected.text` создайте токен:
   - **Откуда брать:** Если старого токена нет, используйте `bondi-blue/850` (Light) или `bondi-blue/200` (Dark) из палитры
   - **Обоснование:** Текст на выбранном чипе должен контрастировать с фоном
6. В `disabled.background` создайте токен:
   - **Откуда брать:** Если старого токена нет, используйте `cool-gray/100` (Light) или `nile-blue/850` (Dark) из палитры
7. В `disabled.text` создайте токен:
   - **Откуда брать:** Если старого токена нет, используйте `cool-gray/400` (Light) или `cool-gray/700` (Dark) из палитры

**Результат:** Токены для Chip созданы

**Проверка:**
- ✅ Структура `color.chip.*` создана
- ✅ Токены для всех вариантов созданы
- ✅ Значения соответствуют таблице соответствия

---

### 2. Checkbox

**Структура:**
```
color.checkbox.default.background.default
color.checkbox.default.border.default
color.checkbox.checked.background.default
color.checkbox.checked.border.default
color.checkbox.disabled.background.default
```

**Действие:**
1. Создайте группу `color.checkbox`
2. Создайте подгруппы: `default`, `checked`, `disabled`
3. В каждой создайте `background` и `border`
4. Значения: найдите в текущих токенах или создайте новые

**Результат:** Токены для Checkbox созданы

**Проверка:**
- ✅ Структура `color.checkbox.*` создана
- ✅ Токены для всех состояний созданы

---

### 3. Toggle

**Структура:**
```
color.toggle.default.background.default
color.toggle.checked.background.default
color.toggle.disabled.background.default
```

**Действие:**
1. Создайте группу `color.toggle`
2. Создайте подгруппы: `default`, `checked`, `disabled`
3. В каждой создайте `background.default`
4. Значения: найдите в текущих токенах или создайте новые

**Результат:** Токены для Toggle созданы

**Проверка:**
- ✅ Структура `color.toggle.*` создана

---

### 4. Message

**Структура:**
```
color.message.info.background.default
color.message.info.text.default
color.message.error.background.default
color.message.error.text.default
```

**Действие:**
1. Создайте группу `color.message`
2. Создайте подгруппы для ролей: `info`, `error`, `warning`, `success`
3. В каждой создайте `background` и `text`
4. Значения: найдите в текущих токенах или создайте новые

**Результат:** Токены для Message созданы

**Проверка:**
- ✅ Структура `color.message.*` создана

---

### 5. Toast

**Структура:**
```
color.toast.success.background.default
color.toast.success.text.default
color.toast.error.background.default
```

**Действие:**
1. Создайте группу `color.toast`
2. Создайте подгруппы для ролей: `success`, `error`, `warning`, `info`
3. В каждой создайте `background` и `text`
4. Значения: найдите в текущих токенах или создайте новые

**Результат:** Токены для Toast созданы

**Проверка:**
- ✅ Структура `color.toast.*` создана

---

### 6. Tooltip

**Структура:**
```
color.tooltip.default.background.default
color.tooltip.default.text.default
```

**Действие:**
1. Создайте группу `color.tooltip`
2. Создайте подгруппу `default`
3. Создайте `background` и `text`
4. Значения: найдите в текущих токенах или создайте новые

**Результат:** Токены для Tooltip созданы

**Проверка:**
- ✅ Структура `color.tooltip.*` создана

---

### 7. DataTable

**📊 Используйте таблицу соответствия:** [TOKEN_MAPPING_TABLES.md#datatable](TOKEN_MAPPING_TABLES.md#datatable)

**Структура:**
```
color.table.header.background.default
color.table.header.text.default
color.table.row.background.default
color.table.row.background.hover
color.table.row.background.selected
color.table.row.text.default
color.table.row.text.selected
```

**Действие:**
1. Создайте группу `color.table`
2. Создайте подгруппы: `header`, `row`
3. В `header.background` создайте токен:
   - **Откуда брать:** Скопируйте значение из `Background.header.table` или `Background.card.header-table`
   - Проверьте оба токена, используйте подходящий
4. В `header.text` создайте токен:
   - **Откуда брать:** Если старого токена нет, используйте `cool-gray/800` (Light) или `cool-gray/200` (Dark) из палитры
   - **Обоснование:** Темный текст на светлом фоне заголовка для контраста
5. В `row.background.default` создайте токен:
   - **Откуда брать:** Скопируйте значение из `Background.card.level-1`
   - **Альтернатива:** Используйте `white/950` (Light) или `nile-blue/850` (Dark) из палитры
6. В `row.background.hover` создайте токен:
   - **Откуда брать:** Скопируйте значение из `Background.card.hover`
   - **Альтернатива:** Используйте `cool-gray/100` (Light) или `nile-blue/750` (Dark) из палитры
7. В `row.background.selected` создайте токен:
   - **Откуда брать:** Если старого токена нет, используйте `bondi-blue/100` (Light) или `bondi-blue/800` (Dark) из палитры
   - **Обоснование:** Выбранная строка должна иметь акцентный цвет для визуального выделения
8. В `row.text.default` создайте токен:
   - **Откуда брать:** Если старого токена нет, используйте `cool-gray/800` (Light) или `cool-gray/200` (Dark) из палитры
9. В `row.text.selected` создайте токен:
   - **Откуда брать:** Если старого токена нет, используйте `bondi-blue/850` (Light) или `bondi-blue/200` (Dark) из палитры

**Результат:** Токены для DataTable созданы

**Проверка:**
- ✅ Структура `color.table.*` создана
- ✅ Значения соответствуют таблице соответствия

---

### 8. Spinner

**Структура:**
```
color.spinner.default.fill.default
```

**Действие:**
1. Создайте группу `color.spinner`
2. Создайте подгруппу `default`
3. Создайте `fill.default`
4. Значение: найдите в текущих токенах или создайте новое

**Результат:** Токены для Spinner созданы

**Проверка:**
- ✅ Структура `color.spinner.*` создана

---

## ✅ Чеклист готовности

Перед переходом к следующему шагу убедитесь:

- [ ] Токены созданы для всех компонентов
- [ ] Токены созданы для режимов Light и Dark
- [ ] Структура соответствует схеме `color.{component}.{variant}.{item}.{state}`
- [ ] Значения правильные

---

## 🎯 Следующий шаг

После создания всех токенов переходите к миграции компонентов:

**→ [07-migrate-other-components.md](07-migrate-other-components.md)**

---

## 📝 Заметки

Запишите здесь свои наблюдения:

```
Созданные токены:
- color.chip.* ✅
- color.checkbox.* ✅
- color.toggle.* ✅
- ...

Компоненты, для которых не нашлось старых токенов:
- 
- 
```

