# Шаг 4: Создание токенов для остальных компонентов

**Цель:** Создать новые токены для Chip, Checkbox, Toggle, Message, Toast, Tooltip, DataTable, Spinner.

## 📋 Компоненты для создания токенов

**Приоритет 3:**
- Chip
- Checkbox
- Toggle

**Приоритет 4:**
- Message
- Toast
- Tooltip

**Приоритет 5:**
- DataTable
- Spinner

## Общий подход

Для каждого компонента:
1. Создать группу `color.{component}`
2. Создать подгруппы для вариантов/ролей
3. Создать токены для background, text, border (где нужно)
4. Создать для режимов Light и Dark

## Что нужно сделать

### 1. Chip

**Структура:**
```
color.chip.default.background.default
color.chip.selected.background.default
color.chip.disabled.background.default
```

**Действие:**
1. Создайте группу `color.chip`
2. Создайте подгруппы: `default`, `selected`, `disabled`
3. В каждой подгруппе создайте `background.default`
4. Значения: используйте `Background.chips.default` и создайте для selected/disabled

**Результат:** Токены для Chip созданы

**Проверка:**
- ✅ Структура `color.chip.*` создана
- ✅ Токены для всех вариантов созданы

---

### 2. Checkbox

**Структура:**
```
color.checkbox.default.background.default
color.checkbox.default.border.default
color.checkbox.checked.background.default
color.checkbox.checked.border.default
color.checkbox.disabled.background.default
```

**Действие:**
1. Создайте группу `color.checkbox`
2. Создайте подгруппы: `default`, `checked`, `disabled`
3. В каждой создайте `background` и `border`
4. Значения: найдите в текущих токенах или создайте новые

**Результат:** Токены для Checkbox созданы

**Проверка:**
- ✅ Структура `color.checkbox.*` создана
- ✅ Токены для всех состояний созданы

---

### 3. Toggle

**Структура:**
```
color.toggle.default.background.default
color.toggle.checked.background.default
color.toggle.disabled.background.default
```

**Действие:**
1. Создайте группу `color.toggle`
2. Создайте подгруппы: `default`, `checked`, `disabled`
3. В каждой создайте `background.default`
4. Значения: найдите в текущих токенах или создайте новые

**Результат:** Токены для Toggle созданы

**Проверка:**
- ✅ Структура `color.toggle.*` создана

---

### 4. Message

**Структура:**
```
color.message.info.background.default
color.message.info.text.default
color.message.error.background.default
color.message.error.text.default
```

**Действие:**
1. Создайте группу `color.message`
2. Создайте подгруппы для ролей: `info`, `error`, `warning`, `success`
3. В каждой создайте `background` и `text`
4. Значения: найдите в текущих токенах или создайте новые

**Результат:** Токены для Message созданы

**Проверка:**
- ✅ Структура `color.message.*` создана

---

### 5. Toast

**Структура:**
```
color.toast.success.background.default
color.toast.success.text.default
color.toast.error.background.default
```

**Действие:**
1. Создайте группу `color.toast`
2. Создайте подгруппы для ролей: `success`, `error`, `warning`, `info`
3. В каждой создайте `background` и `text`
4. Значения: найдите в текущих токенах или создайте новые

**Результат:** Токены для Toast созданы

**Проверка:**
- ✅ Структура `color.toast.*` создана

---

### 6. Tooltip

**Структура:**
```
color.tooltip.default.background.default
color.tooltip.default.text.default
```

**Действие:**
1. Создайте группу `color.tooltip`
2. Создайте подгруппу `default`
3. Создайте `background` и `text`
4. Значения: найдите в текущих токенах или создайте новые

**Результат:** Токены для Tooltip созданы

**Проверка:**
- ✅ Структура `color.tooltip.*` создана

---

### 7. DataTable

**Структура:**
```
color.table.header.background.default
color.table.header.text.default
color.table.row.background.default
color.table.row.background.hover
color.table.row.background.selected
```

**Действие:**
1. Создайте группу `color.table`
2. Создайте подгруппы: `header`, `row`
3. В `header` создайте `background` и `text`
4. В `row` создайте `background` для состояний: `default`, `hover`, `selected`
5. Значения: используйте `Background.card.*` и `Background.header.*`

**Результат:** Токены для DataTable созданы

**Проверка:**
- ✅ Структура `color.table.*` создана

---

### 8. Spinner

**Структура:**
```
color.spinner.default.fill.default
```

**Действие:**
1. Создайте группу `color.spinner`
2. Создайте подгруппу `default`
3. Создайте `fill.default`
4. Значение: найдите в текущих токенах или создайте новое

**Результат:** Токены для Spinner созданы

**Проверка:**
- ✅ Структура `color.spinner.*` создана

---

## ✅ Чеклист готовности

Перед переходом к следующему шагу убедитесь:

- [ ] Токены созданы для всех компонентов
- [ ] Токены созданы для режимов Light и Dark
- [ ] Структура соответствует схеме `color.{component}.{variant}.{item}.{state}`
- [ ] Значения правильные

---

## 🎯 Следующий шаг

После создания всех токенов переходите к миграции компонентов:

**→ [07-migrate-other-components.md](07-migrate-other-components.md)**

---

## 📝 Заметки

Запишите здесь свои наблюдения:

```
Созданные токены:
- color.chip.* ✅
- color.checkbox.* ✅
- color.toggle.* ✅
- ...

Компоненты, для которых не нашлось старых токенов:
- 
- 
```

