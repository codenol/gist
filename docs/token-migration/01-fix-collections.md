# Шаг 1: Исправление структуры коллекций в Figma

**Цель:** Убедиться, что темы (Light/Dark) организованы как режимы (modes) в коллекции, а не как отдельные коллекции.

## ⚠️ Проблема

Сейчас коллекция "Color" может содержать темы на неправильном уровне. Темы должны быть **режимами (modes)** внутри коллекции, а не отдельными коллекциями.

## Что нужно сделать

### 1. Проверить текущую структуру

**Действие:**
1. Откройте Figma
2. Перейдите в меню: `Window` → `Variables` (или `Ctrl/Cmd + Alt + K`)
3. Найдите коллекцию "Color"
4. Проверьте, есть ли у неё режимы (modes)

**Как проверить:**
- Если справа от названия коллекции есть выпадающий список с "Light" и "Dark" - режимы уже настроены ✅
- Если нет выпадающего списка - нужно создать режимы

**Результат:** Вы понимаете текущую структуру коллекции

**Проверка:**
- ✅ Вы видите коллекцию "Color"
- ✅ Вы понимаете, есть ли у неё режимы

---

### 2. Создать режимы (если их нет)

**Действие:**
1. Кликните на коллекцию "Color"
2. В правом верхнем углу найдите кнопку "Mode" или "Add mode"
3. Создайте режим "Light" (если его нет)
4. Создайте режим "Dark" (если его нет)

**Результат:** У коллекции "Color" есть два режима: "Light" и "Dark"

**Проверка:**
- ✅ В интерфейсе коллекции видно переключатель между "Light" и "Dark"
- ✅ Можно переключаться между режимами

---

### 3. Организовать токены по режимам

**Действие:**
1. Переключитесь на режим "Light"
2. Убедитесь, что все токены для светлой темы находятся в этом режиме
3. Переключитесь на режим "Dark"
4. Убедитесь, что все токены для темной темы находятся в этом режиме

**Если токены в неправильных режимах:**
1. Выберите токен
2. В свойствах токена найдите поле "Mode" или "Mode value"
3. Установите правильное значение для каждого режима

**Результат:** Все токены правильно распределены по режимам

**Проверка:**
- ✅ В режиме "Light" видны только токены для светлой темы
- ✅ В режиме "Dark" видны только токены для темной темы
- ✅ Каждый токен имеет правильное значение для каждого режима

---

### 4. Проверить структуру других коллекций

**Действие:**
Проверьте коллекции "Dimensions" и "Pallet":
1. Убедитесь, что у них тоже есть режимы (если нужно)
2. Проверьте, что токены правильно распределены

**Результат:** Все коллекции правильно организованы

**Проверка:**
- ✅ Коллекция "Dimensions" организована правильно
- ✅ Коллекция "Pallet" организована правильно

---

## ✅ Чеклист готовности

Перед переходом к следующему шагу убедитесь:

- [ ] Коллекция "Color" имеет режимы "Light" и "Dark"
- [ ] Все токены правильно распределены по режимам
- [ ] Можно переключаться между режимами
- [ ] Другие коллекции тоже правильно организованы

---

## 🎯 Следующий шаг

После исправления структуры коллекций переходите к созданию токенов для Button:

**→ [02-create-button-tokens.md](02-create-button-tokens.md)**

---

## 📝 Заметки

Запишите здесь свои наблюдения:

```
Проблемы, которые были обнаружены:
- 
- 

Что было исправлено:
- 
- 
```

