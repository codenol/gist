# Шаг 2: Создание токенов для Button

**Цель:** Создать новые токены для кнопок по структуре `color.button.filled.{role}.{item}.{state}`

## 📋 Что нужно знать

**Новая структура:**
```
color.button.filled.accent.background.default
color.button.filled.accent.background.hover
color.button.filled.accent.background.active
color.button.filled.accent.text.default
color.button.filled.accent.border.focus
```

**Старая структура:**
```
Button.primary.accent.default-bg
Button.primary.accent.hover-bg
Button.primary.accent.press-bg
```

## Что нужно сделать

### 1. Создать группу токенов

**Действие:**
1. В Figma Variables откройте коллекцию "Color"
2. Переключитесь на режим "Light"
3. Создайте новую группу: кликните правой кнопкой → "Add variable" → выберите тип "Color"
4. Назовите группу: `color.button`

**Результат:** Создана группа `color.button` в коллекции "Color"

**Проверка:**
- ✅ В списке токенов видна группа `color.button`
- ✅ Группа находится в режиме "Light"

---

### 2. Создать подгруппу для варианта "filled"

**Действие:**
1. Внутри группы `color.button` создайте подгруппу `filled`
2. Внутри `filled` создайте подгруппу `accent`
3. Внутри `accent` создайте подгруппу `background`

**Результат:** Структура `color.button.filled.accent.background`

**Проверка:**
- ✅ Видна иерархия: `color.button` → `filled` → `accent` → `background`

---

### 3. Создать токены для background (accent, режим Light)

**Действие:**
Внутри `color.button.filled.accent.background` создайте токены:

1. **default:**
   - Название: `default`
   - Значение: скопируйте из `Button.primary.accent.default-bg`
   - Режим: Light

2. **hover:**
   - Название: `hover`
   - Значение: скопируйте из `Button.primary.accent.hover-bg`
   - Режим: Light

3. **active:**
   - Название: `active`
   - Значение: скопируйте из `Button.primary.accent.press-bg`
   - Режим: Light

4. **disabled:**
   - Название: `disabled`
   - Значение: скопируйте из `Button.primary.accent.disabled-bg`
   - Режим: Light

**Результат:** Токены `color.button.filled.accent.background.default/hover/active/disabled` для режима Light

**Проверка:**
- ✅ Все 4 токена созданы
- ✅ Значения совпадают со старыми токенами
- ✅ Токены находятся в режиме "Light"

---

### 4. Создать токены для режима Dark

**Действие:**
1. Переключитесь на режим "Dark"
2. Повторите шаг 3, но используйте значения из `Current/Color/Dark.tokens.json`
3. Убедитесь, что токены находятся в режиме "Dark"

**Результат:** Токены для режима Dark созданы

**Проверка:**
- ✅ Все токены созданы для режима "Dark"
- ✅ Значения правильные для темной темы

---

### 5. Создать токены для text (accent)

**Действие:**
1. В режиме "Light" создайте структуру: `color.button.filled.accent.text`
2. Создайте токены:
   - `default` - белый цвет (#FFFFFF) для Light
   - `disabled` - скопируйте из `Button.primary.accent.disabled-text`
3. Повторите для режима "Dark"

**Результат:** Токены `color.button.filled.accent.text.default/disabled`

**Проверка:**
- ✅ Токены text созданы для обоих режимов

---

### 6. Создать токены для border (accent)

**Действие:**
1. В режиме "Light" создайте структуру: `color.button.filled.accent.border`
2. Создайте токены:
   - `default` - обычно совпадает с background или прозрачный
   - `focus` - скопируйте из `Button.primary.accent.focus-stroke`
3. Повторите для режима "Dark"

**Результат:** Токены `color.button.filled.accent.border.default/focus`

**Проверка:**
- ✅ Токены border созданы для обоих режимов

---

### 7. Повторить для всех ролей

**Действие:**
Повторите шаги 2-6 для всех ролей:
- `danger` (из `Button.primary.danger.*`)
- `warning` (из `Button.primary.warning.*`)
- `success` (из `Button.primary.success.*`)
- `info` (из `Button.primary.info.*`)
- `neutral` (из `Button.primary.neutral.*`)

**Результат:** Все токены для `color.button.filled.{role}.*` созданы

**Проверка:**
- ✅ Токены созданы для всех ролей
- ✅ Для каждой роли есть background, text, border

---

### 8. Создать токены для secondary и tertiary (если есть)

**Действие:**
Если в вашей системе есть secondary и tertiary кнопки:
1. Создайте структуру `color.button.outline.*` для secondary
2. Создайте структуру `color.button.ghost.*` для tertiary
3. Повторите шаги 2-7 для этих вариантов

**Результат:** Токены для всех вариантов кнопок

**Проверка:**
- ✅ Токены созданы для всех вариантов

---

## ✅ Чеклист готовности

Перед переходом к следующему шагу убедитесь:

- [ ] Все токены для `color.button.filled.*` созданы
- [ ] Токены созданы для режимов Light и Dark
- [ ] Для каждой роли есть background, text, border
- [ ] Значения совпадают со старыми токенами
- [ ] Структура соответствует `color.button.filled.{role}.{item}.{state}`

---

## 🎯 Следующий шаг

После создания всех токенов для Button переходите к миграции компонентов Button:

**→ [05-migrate-button.md](05-migrate-button.md)**

**Примечание:** Мы мигрируем Button сразу после создания токенов, так как это приоритетный компонент. После миграции Button переходим к созданию токенов для других компонентов.

---

## 📝 Заметки

Запишите здесь свои наблюдения:

```
Созданные токены:
- color.button.filled.accent.*
- color.button.filled.danger.*
- ...

Проблемы, которые возникли:
- 
- 
```

