# Шаг 3: Создание токенов для Badge и Status

**Цель:** Создать новые токены для Badge и Status по структуре `color.badge.{role}.{item}.{state}` и `color.status.{role}.{item}.{state}`

## 📋 Что нужно знать

**Новая структура Badge:**
```
color.badge.neutral.background.default
color.badge.neutral.text.default
color.badge.neutral.border.default
```

**Старая структура Badge:**
```
Badges.gray.surface
Badges.gray.text
Badges.gray.stroke
```

**Новая структура Status:**
```
color.status.critical.background.default
color.status.critical.text.default
```

**Старая структура Status:**
```
Status.critical.surface
Status.critical.text
```

## Что нужно сделать

### 1. Создать группу токенов для Badge

**Действие:**
1. В Figma Variables откройте коллекцию "Color"
2. Переключитесь на режим "Light"
3. Создайте новую группу: `color.badge`

**Результат:** Создана группа `color.badge`

**Проверка:**
- ✅ В списке токенов видна группа `color.badge`

---

### 2. Создать токены для Badge neutral (Light)

**Действие:**
1. Внутри `color.badge` создайте подгруппу `neutral`
2. Создайте подгруппы: `background`, `text`, `border`
3. В `background` создайте токен:
   - Название: `default`
   - Значение: скопируйте из `Badges.gray.surface`
   - Режим: Light
4. В `text` создайте токен:
   - Название: `default`
   - Значение: скопируйте из `Badges.gray.text`
   - Режим: Light
5. В `border` создайте токен:
   - Название: `default`
   - Значение: скопируйте из `Badges.gray.stroke`
   - Режим: Light

**Результат:** Токены `color.badge.neutral.*` для режима Light

**Проверка:**
- ✅ Все токены созданы
- ✅ Значения совпадают со старыми

---

### 3. Создать токены для Badge neutral (Dark)

**Действие:**
1. Переключитесь на режим "Dark"
2. Повторите шаг 2, используя значения из `Current/Color/Dark.tokens.json`

**Результат:** Токены для режима Dark созданы

**Проверка:**
- ✅ Токены созданы для режима "Dark"

---

### 4. Повторить для всех ролей Badge

**Действие:**
Повторите шаги 2-3 для всех ролей:
- `accent` (из `Badges.accent.*`)
- `danger` (из `Badges.danger.*`)
- `warning` (из `Badges.warning.*`)
- `success` (из `Badges.success.*`)
- `info` (из `Badges.info.*`)
- `gray-warm` → можно объединить с `neutral` или создать `neutral.warm`

**Результат:** Все токены для Badge созданы

**Проверка:**
- ✅ Токены созданы для всех ролей
- ✅ Для каждой роли есть background, text, border

---

### 5. Создать группу токенов для Status

**Действие:**
1. В режиме "Light" создайте группу: `color.status`

**Результат:** Создана группа `color.status`

**Проверка:**
- ✅ В списке токенов видна группа `color.status`

---

### 6. Создать токены для Status critical (Light)

**Действие:**
1. Внутри `color.status` создайте подгруппу `critical`
2. Создайте подгруппы: `background`, `text`
3. В `background` создайте токен:
   - Название: `default`
   - Значение: скопируйте из `Status.critical.surface`
   - Режим: Light
4. В `text` создайте токен:
   - Название: `default`
   - Значение: скопируйте из `Status.critical.text`
   - Режим: Light

**Результат:** Токены `color.status.critical.*` для режима Light

**Проверка:**
- ✅ Все токены созданы
- ✅ Значения совпадают со старыми

---

### 7. Создать токены для Status critical (Dark)

**Действие:**
1. Переключитесь на режим "Dark"
2. Повторите шаг 6, используя значения из `Current/Color/Dark.tokens.json`

**Результат:** Токены для режима Dark созданы

**Проверка:**
- ✅ Токены созданы для режима "Dark"

---

### 8. Повторить для всех статусов

**Действие:**
Повторите шаги 6-7 для всех статусов:
- `warning` (из `Status.warning.*`)
- `warning-strong` → можно создать как `color.status.warning.background.strong`
- `success` (из `Status.success.*`)
- `info` (из `Status.info.*`)
- `neutral` (из `Status.neutral.*`)

**Результат:** Все токены для Status созданы

**Проверка:**
- ✅ Токены созданы для всех статусов
- ✅ Для каждого статуса есть background и text

---

## ✅ Чеклист готовности

Перед переходом к следующему шагу убедитесь:

- [ ] Все токены для `color.badge.*` созданы
- [ ] Все токены для `color.status.*` созданы
- [ ] Токены созданы для режимов Light и Dark
- [ ] Значения совпадают со старыми токенами
- [ ] Структура соответствует новой схеме

---

## 🎯 Следующий шаг

После создания всех токенов переходите к миграции компонентов:

**→ [06-migrate-badge-status.md](06-migrate-badge-status.md)**

---

## 📝 Заметки

Запишите здесь свои наблюдения:

```
Созданные токены Badge:
- color.badge.neutral.*
- color.badge.accent.*
- ...

Созданные токены Status:
- color.status.critical.*
- color.status.warning.*
- ...

Проблемы, которые возникли:
- 
- 
```

