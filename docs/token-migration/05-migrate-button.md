# Шаг 5: Миграция компонентов Button

**Цель:** Обновить компоненты кнопок, чтобы они использовали новые токены вместо старых.

## ⚠️ Важно

- Мигрируйте компоненты **по одному**
- Проверяйте результат после каждого компонента
- Не удаляйте старые токены до полной миграции

## Что нужно сделать

### 1. Выбрать первый компонент для миграции

**Действие:**
1. Откройте компонент Button в Figma
2. Выберите вариант "Primary Accent" (самый используемый)
3. Откройте свойства компонента

**Результат:** Вы готовы мигрировать первый компонент

**Проверка:**
- ✅ Компонент открыт
- ✅ Вы видите все свойства компонента

---

### 2. Заменить токены background

**Действие:**
В свойствах компонента найдите все использования старых токенов и замените:

1. **Default состояние:**
   - Старый: `Button.primary.accent.default-bg`
   - Новый: `color.button.filled.accent.background.default`
   - Действие: Выберите свойство "Fill" → выберите новый токен

2. **Hover состояние:**
   - Старый: `Button.primary.accent.hover-bg`
   - Новый: `color.button.filled.accent.background.hover`
   - Действие: Выберите состояние "Hover" → свойство "Fill" → новый токен

3. **Active/Press состояние:**
   - Старый: `Button.primary.accent.press-bg`
   - Новый: `color.button.filled.accent.background.active`
   - Действие: Выберите состояние "Pressed" → свойство "Fill" → новый токен

4. **Disabled состояние:**
   - Старый: `Button.primary.accent.disabled-bg`
   - Новый: `color.button.filled.accent.background.disabled`
   - Действие: Выберите состояние "Disabled" → свойство "Fill" → новый токен

**Результат:** Все токены background заменены на новые

**Проверка:**
- ✅ Все состояния используют новые токены
- ✅ Визуально компонент выглядит так же
- ✅ Нет ошибок в Figma

---

### 3. Заменить токены text

**Действие:**
1. Найдите текстовый слой в компоненте
2. Замените токены:

   - **Default:**
     - Старый: (может не быть явного токена)
     - Новый: `color.button.filled.accent.text.default`
     - Действие: Выберите свойство "Fill" текста → новый токен

   - **Disabled:**
     - Старый: `Button.primary.accent.disabled-text`
     - Новый: `color.button.filled.accent.text.disabled`
     - Действие: Выберите состояние "Disabled" → свойство "Fill" текста → новый токен

**Результат:** Все токены text заменены

**Проверка:**
- ✅ Текст использует новые токены
- ✅ Цвет текста правильный во всех состояниях

---

### 4. Заменить токены border

**Действие:**
1. Найдите свойство "Stroke" компонента
2. Замените токены:

   - **Default:**
     - Новый: `color.button.filled.accent.border.default`
     - Действие: Выберите свойство "Stroke" → новый токен

   - **Focus:**
     - Старый: `Button.primary.accent.focus-stroke`
     - Новый: `color.button.filled.accent.border.focus`
     - Действие: Выберите состояние "Focus" → свойство "Stroke" → новый токен

**Результат:** Все токены border заменены

**Проверка:**
- ✅ Border использует новые токены
- ✅ Focus состояние работает правильно

---

### 5. Проверить компонент

**Действие:**
1. Переключитесь между режимами Light/Dark
2. Проверьте все состояния:
   - Default
   - Hover
   - Active/Pressed
   - Disabled
   - Focus
3. Убедитесь, что визуально всё выглядит правильно

**Результат:** Компонент работает корректно

**Проверка:**
- ✅ Все состояния работают
- ✅ Визуально выглядит так же, как до миграции
- ✅ Нет ошибок в Figma
- ✅ Работает в обоих режимах (Light/Dark)

---

### 6. Проверить использование в макетах

**Действие:**
1. Найдите все экземпляры этого компонента в макетах
2. Проверьте несколько экземпляров:
   - Откройте макет
   - Найдите экземпляры компонента
   - Убедитесь, что они отображаются правильно

**Результат:** Все экземпляры работают корректно

**Проверка:**
- ✅ Экземпляры компонента отображаются правильно
- ✅ Нет визуальных изменений
- ✅ Нет ошибок

---

### 7. Повторить для остальных вариантов

**Действие:**
Повторите шаги 1-6 для всех остальных вариантов кнопок:

1. Primary Danger
2. Primary Warning
3. Primary Success
4. Primary Info
5. Primary Neutral
6. Secondary (все роли)
7. Tertiary (все роли, если есть)

**Результат:** Все варианты кнопок мигрированы

**Проверка:**
- ✅ Все варианты мигрированы
- ✅ Все работают корректно

---

## ✅ Чеклист готовности

Перед переходом к следующему шагу убедитесь:

- [ ] Все варианты Button мигрированы
- [ ] Все состояния работают корректно
- [ ] Компоненты работают в обоих режимах (Light/Dark)
- [ ] Экземпляры в макетах работают правильно
- [ ] Нет визуальных изменений
- [ ] Нет ошибок в Figma

---

## 🎯 Следующий шаг

После миграции всех кнопок переходите к созданию токенов для Badge и Status (Приоритет 2):

**→ [03-create-badge-status-tokens.md](03-create-badge-status-tokens.md)**

---

## 📝 Заметки

Запишите здесь свои наблюдения:

```
Мигрированные компоненты:
- Primary Accent ✅
- Primary Danger ✅
- ...

Проблемы, которые возникли:
- 
- 

Что нужно проверить дополнительно:
- 
- 
```

