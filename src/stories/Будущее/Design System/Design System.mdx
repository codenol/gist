import { Meta, Story } from '@storybook/addon-docs/blocks';
import * as DesignSystemStories from './Design System.stories';

<Meta title="Будущее/Design System" of={DesignSystemStories} />

# Design System

**Идеальная система дизайн-токенов для Vision Platform**

Добро пожаловать в систему дизайн-токенов! Здесь вы найдете все токены, паттерны и примеры использования для создания консистентного интерфейса.

## 🎯 Что такое Design Tokens?

Design Tokens — это атомарные значения дизайна (цвета, отступы, размеры шрифтов, скругления), которые используются как единый источник правды для всей дизайн-системы. Все токены хранятся в формате [W3C Design Tokens](https://tr.designtokens.org/format/) и автоматически генерируются в CSS переменные.

## 📚 Структура системы

### 📋 [All Tokens](./All%20Tokens) — Полный справочник

**Начните здесь!** Полный список всех токенов с таблицами, значениями и CSS переменными.

### Foundations (Основы)

Базовые токены, которые используются во всех компонентах:

- **[Spacing](./Foundations/Spacing)** — Система отступов
  - Примитивы: 2px, 4px, 8px, 12px, 16px, 20px, 24px, 28px
  - Семантические: interactive-padding, field-padding, container-padding
  
- **[BorderRadius](./Foundations/BorderRadius)** — Система скруглений
  - Примитивы: xs (4px), s (6px), m (8px), l (10px), xl (12px), full (999px)
  - Семантические: interactive-default, field-default, surface-default
  
- **[Typography](./Foundations/Typography)** — Типографика
  - Размеры: xs (10px), s (12px), m (14px), l (16px), xl (18px), 2xl (20px), 3xl (24px)
  - Веса: regular (400), medium (500), semibold (600), bold (700)
  - Высоты строк: tight (1.2), normal (1.5), relaxed (1.75)
  
- **[Colors](./Foundations/Colors)** — Цветовая система
  - Примитивы: accent, neutral, error
  - Семантические: interactive, surface, border, text

### Layout (Макеты)

Паттерны компоновки с использованием токенов:

- **[Grid](./Layout/Grid)** — Сеточные макеты
- **[Containers](./Layout/Containers)** — Контейнеры и обертки
- **[Stack](./Layout/Stack)** — Вертикальные стеки
- **[Flex](./Layout/Flex)** — Flexbox макеты

### Examples (Примеры)

Реальные примеры применения токенов в компонентах:

- **[Button](./Examples/Button)** — Кнопки с применением всех токенов
- **[Input](./Examples/Input)** — Поля ввода с применением всех токенов

## 🚀 Быстрый старт

### Как использовать токены в коде

Все токены доступны как CSS переменные:

```tsx
// ✅ Правильно: используем токены
<button style={{
  padding: 'var(--spacing-semantic-interactive-padding-y) var(--spacing-semantic-interactive-padding-x)',
  borderRadius: 'var(--radius-semantic-interactive-default)',
  backgroundColor: 'var(--color-semantic-interactive-accent-default)',
  color: 'var(--color-semantic-text-on-accent)',
}}>
  Кнопка
</button>

// ❌ Неправильно: hardcoded значения
<button style={{
  padding: '8px 16px',
  borderRadius: '8px',
  backgroundColor: '#00BEC8',
  color: '#ffffff',
}}>
  Кнопка
</button>
```

### Использование в SCSS

```scss
.my-component {
  padding: var(--spacing-semantic-container-padding);
  border-radius: var(--radius-semantic-surface-default);
  background-color: var(--color-semantic-surface-interactive-default);
  color: var(--color-semantic-text-on-surface);
}
```

## 📖 Документация токенов

### JSON файл

Все токены определены в `src/design-tokens/design-tokens.json` в формате W3C Design Tokens.

### CSS переменные

Все токены автоматически генерируются в `src/stories/Будущее/Design System/tokens.scss` и импортируются в Storybook через `preview.jsx`.

### TypeScript утилиты

Для работы с токенами в TypeScript используйте `src/design-tokens/tokenParser.ts`:

```typescript
import { loadDesignTokens, getTokenValue } from '../../../design-tokens/tokenParser';

const tokens = loadDesignTokens();
const spacingValue = getTokenValue('spacing.primitives.200'); // "8px"
const semanticSpacing = getTokenValue('spacing.semantic.interactive.padding-x'); // "16px"
```

## 🎨 Принципы использования

1. **Всегда используйте токены** — никогда не используйте hardcoded значения (px, цвета, размеры)
2. **Приоритет семантических токенов** — используйте семантические токены вместо примитивов, когда это возможно
3. **Следуйте паттернам** — используйте примеры из Layout и Examples разделов
4. **Проверяйте в Storybook** — все токены можно посмотреть и протестировать в соответствующих stories

## 🔗 Полезные ссылки

- [W3C Design Tokens Format](https://tr.designtokens.org/format/)
- [Design Tokens Community Group](https://www.designtokens.org/)
- [Token Studio](https://tokens.studio/) — инструмент для работы с токенами

## 📝 Структура файлов

```
src/
├── design-tokens/
│   ├── design-tokens.json      # JSON файл с токенами (W3C формат)
│   └── tokenParser.ts          # Утилиты для работы с токенами
└── stories/
    └── Будущее/
        └── Design System/
            ├── Design System.mdx    # Эта страница
            ├── tokens.scss          # Сгенерированные CSS переменные
            ├── Foundations/         # Базовые токены
            ├── Layout/              # Паттерны макетов
            └── Examples/            # Примеры применения
```

---

**Начните с раздела [Foundations](./Foundations/Spacing) для изучения базовых токенов!**

