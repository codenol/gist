# Инструкция по компонентному подходу к токенам

## Для кого эта инструкция?

Эта инструкция создана для **дизайнеров дизайн-системы**, которые создают и используют компонентные токены в своей работе. Она показывает, как именно мы будем использовать токены на практике.

## Что такое компонентный подход?

Компонентный подход — это способ организации токенов, где каждый токен привязан к конкретному компоненту. Структура токена выглядит так:

```
[Category].[Component].[Variant].[Subcomponent].[Item].[State]
```

Где:
- **Category** — категория токена (color, spacing, typography, size, border_radius, shadow)
- **Component** — название компонента (button, input, table, badge)
- **Variant** — вариант компонента (filled, outline, ghost для кнопки; success, error для бейджа)
- **Subcomponent** — компонент внутри большого составного компонента (например, кнопка в форме, header в таблице, icon в кнопке)
- **Item** — элемент компонента (background, text, border, icon, label)
- **State** — состояние элемента (default, hover, active, disabled, focus, error)

**Примечание:** Для простых компонентов без subcomponent структура может быть упрощена до `[Category].[Component].[Variant].[Item].[State]`

## Примеры структуры

- `color.button.filled.background.default` — цвет фона кнопки варианта "filled" в состоянии по умолчанию
- `color.button.filled.text.default` — цвет текста кнопки варианта "filled" в состоянии по умолчанию
- `color.button.filled.icon.hover` — цвет иконки в кнопке варианта "filled" при наведении (icon — это subcomponent)
- `color.table.default.header.background.default` — цвет фона заголовка таблицы (header — это subcomponent)
- `spacing.input.default.padding.x` — горизонтальный отступ инпута (если нет вариантов, можно использовать "default")
- `typography.table.default.header.font_size` — размер шрифта заголовка таблицы

## Роли в процессе

В работе с токенами участвуют две роли:

- **Лид дизайн-системы** — отвечает за стратегию токенизации, утверждает структуру токенов, принимает решения о добавлении новых токенов в систему
- **Дизайнер дизайн-системы** — создает компоненты, выбирает и использует токены, вносит изменения и согласовывает их с фронтенд-разработчиками и остальными участниками команды

---

## Пример 1: Кнопка (Button)

### Я как дизайнер хочу создать компонент "button"

#### Что этот компонент из себя представляет?

Кнопка — это основной интерактивный элемент интерфейса, который позволяет пользователю выполнять действия. Кнопка должна быть заметной, понятной и отзывчивой на взаимодействие.

#### Какой он бывает? Какие у него варианты?

Кнопка может быть разных типов:
- **Filled** (заполненная) — основной вариант, с цветным фоном
- **Outline** (контурная) — с прозрачным фоном и цветной границей
- **Ghost** (призрачная) — минималистичная, без фона и границы

У кнопки есть состояния:
- **Default** — обычное состояние
- **Hover** — при наведении курсора
- **Active** — при нажатии
- **Disabled** — неактивное состояние

У кнопки есть размеры:
- **Small** — компактная кнопка
- **Medium** — стандартная кнопка
- **Large** — крупная кнопка

#### Какие элементы у кнопки?

Кнопка состоит из:
- **Background** — фон кнопки
- **Text** (или **Label**) — текст на кнопке
- **Icon** — иконка (опционально)
- **Border** — граница (для outline варианта)
- **Padding** — внутренние отступы
- **Border radius** — скругление углов
- **Shadow** — тень (опционально)

#### Какие я выберу токены?

Для кнопки варианта "filled" я создам следующие токены:

**Цвета:**
- `color.button.filled.background.default` — основной цвет фона (например, синий)
- `color.button.filled.background.hover` — цвет фона при наведении (более темный синий)
- `color.button.filled.background.active` — цвет фона при нажатии (еще более темный)
- `color.button.filled.background.disabled` — цвет фона в неактивном состоянии (серый)
- `color.button.filled.text.default` — цвет текста (белый для контраста)
- `color.button.filled.text.disabled` — цвет текста в disabled состоянии
- `color.button.filled.border.default` — цвет границы (может совпадать с фоном или быть прозрачным)
- `color.button.filled.icon.default` — цвет иконки в кнопке (icon — это subcomponent внутри кнопки)
- `color.button.filled.icon.hover` — цвет иконки при наведении

**Отступы:**
- `spacing.button.padding.x` — горизонтальный отступ (слева и справа)
- `spacing.button.padding.y` — вертикальный отступ (сверху и снизу)
- `spacing.button.gap` — расстояние между иконкой и текстом

**Размеры:**
- `size.button.small.height` — высота маленькой кнопки
- `size.button.medium.height` — высота средней кнопки
- `size.button.large.height` — высота большой кнопки

**Типографика:**
- `typography.button.font_size.small` — размер шрифта для маленькой кнопки
- `typography.button.font_size.medium` — размер шрифта для средней кнопки
- `typography.button.font_size.large` — размер шрифта для большой кнопки
- `typography.button.font_weight` — вес шрифта (обычно medium или semibold)

**Остальное:**
- `border_radius.button` — скругление углов
- `shadow.button.default` — тень в обычном состоянии
- `shadow.button.hover` — тень при наведении

Для кнопки варианта "outline" я создам аналогичные токены, но с вариантом `outline`:
- `color.button.outline.background.default` — прозрачный фон
- `color.button.outline.border.default` — цветная граница
- `color.button.outline.text.default` — цвет текста (обычно совпадает с цветом границы)
- `color.button.outline.icon.default` — цвет иконки в outline кнопке

#### Что буду делать, если токена нет?

Если мне нужен токен, которого еще нет в системе, я:

1. **Обращаюсь к дизайнеру дизайн-системы** — я иду к дизайнеру ДС и объясняю, какой токен мне нужен, почему он нужен и где будет использоваться
2. **Дизайнер ДС создает токен** — дизайнер ДС создает новый токен по структуре `[Category].[Component].[Variant].[Subcomponent].[Item].[State]`, согласовывает его с лидом дизайн-системы, документирует решение и согласовывает с фронтенд-разработчиками

**Пример:** Если мне нужен токен для иконки в кнопке при hover состоянии, я обращаюсь к дизайнеру ДС и объясняю, что нужен токен `color.button.filled.icon.hover` (где `icon` — это subcomponent внутри кнопки). Дизайнер ДС создаст этот токен, согласует с лидом и фронтенд-разработчиками.

---

## Пример 2: Инпут (Input)

### Я как дизайнер хочу создать компонент "input"

#### Что этот компонент из себя представляет?

Инпут — это поле ввода, которое позволяет пользователю вводить текст или данные. Инпут должен быть понятным, доступным и четко показывать свое состояние.

#### Какой он бывает? Какие у него варианты?

Инпут обычно не имеет вариантов (variant), но имеет важные состояния:
- **Default** — обычное состояние
- **Hover** — при наведении курсора
- **Focus** — когда поле в фокусе (пользователь кликнул или перешел по Tab)
- **Error** — когда есть ошибка валидации
- **Disabled** — неактивное состояние

У инпута есть размеры:
- **Small** — компактное поле
- **Medium** — стандартное поле
- **Large** — крупное поле

#### Какие элементы у инпута?

Инпут состоит из:
- **Background** — фон поля
- **Text** — введенный пользователем текст
- **Placeholder** — подсказка (текст-заполнитель)
- **Border** — граница поля
- **Padding** — внутренние отступы
- **Border radius** — скругление углов

#### Какие я выберу токены?

Для инпута я создам следующие токены (так как у инпута нет вариантов, можно использовать "default" или опустить variant):

**Цвета:**
- `color.input.default.background.default` — фон поля (обычно белый)
- `color.input.default.background.hover` — фон при наведении (может быть чуть темнее)
- `color.input.default.background.focus` — фон в фокусе (может быть с легким оттенком)
- `color.input.default.background.error` — фон при ошибке (светло-красный)
- `color.input.default.background.disabled` — фон в disabled состоянии (серый)
- `color.input.default.text.default` — цвет введенного текста (темный)
- `color.input.default.text.placeholder` — цвет placeholder (светло-серый)
- `color.input.default.text.disabled` — цвет текста в disabled состоянии
- `color.input.default.border.default` — цвет границы по умолчанию (светло-серый)
- `color.input.default.border.hover` — цвет границы при наведении
- `color.input.default.border.focus` — цвет границы в фокусе (яркий, обычно брендовый цвет)
- `color.input.default.border.error` — цвет границы при ошибке (красный)
- `color.input.default.border.disabled` — цвет границы в disabled состоянии

**Отступы:**
- `spacing.input.default.padding.x` — горизонтальный отступ (слева и справа)
- `spacing.input.default.padding.y` — вертикальный отступ (сверху и снизу)

**Размеры:**
- `size.input.default.small.height` — высота маленького инпута
- `size.input.default.medium.height` — высота среднего инпута
- `size.input.default.large.height` — высота большого инпута

**Типографика:**
- `typography.input.default.font_size` — размер шрифта
- `typography.input.default.font_weight` — вес шрифта
- `typography.input.default.line_height` — высота строки

**Остальное:**
- `border_radius.input.default` — скругление углов

#### Что буду делать, если токена нет?

Если мне нужен токен для состояния, которого еще нет:

1. **Обращаюсь к дизайнеру дизайн-системы** — я иду к дизайнеру ДС и объясняю, какое состояние мне нужно и почему
2. **Дизайнер ДС создает токен** — дизайнер ДС создает новый токен (например, `color.input.default.border.success` для успешной валидации), проверяет консистентность, согласовывает с лидом и фронтенд-разработчиками

**Пример:** Если мне нужен токен для успешной валидации инпута (зеленая граница), я обращаюсь к дизайнеру ДС и объясняю необходимость. Дизайнер ДС создаст токены `color.input.default.border.success` и `color.input.default.background.success`, согласует с лидом и фронтенд-разработчиками.

---

## Пример 3: Таблица (Table)

### Я как дизайнер хочу создать компонент "table"

#### Что этот компонент из себя представляет?

Таблица — это структурированный способ отображения данных в строках и столбцах. Таблица должна быть читаемой, понятной и помогать пользователю находить нужную информацию.

#### Какой он бывает? Какие у него варианты?

Таблица обычно не имеет вариантов (variant), но состоит из нескольких частей (subcomponents):
- **Header** (заголовок) — строка с названиями столбцов (это subcomponent)
- **Row** (строка) — строка с данными (это subcomponent)
- **Cell** (ячейка) — отдельная ячейка в строке (это subcomponent)

У строк есть состояния:
- **Default** — обычное состояние
- **Hover** — при наведении курсора
- **Selected** — выбранная строка

#### Какие элементы у таблицы?

Таблица состоит из:
- **Header background** — фон заголовка
- **Header text** — текст заголовка
- **Row background** — фон строки
- **Row text** — текст в строке
- **Cell padding** — отступы в ячейках
- **Border** — границы между ячейками и строками
- **Typography** — типографика для заголовка и тела таблицы

#### Какие я выберу токены?

Для таблицы я создам следующие токены (header, row, cell — это subcomponents):

**Цвета для заголовка:**
- `color.table.default.header.background.default` — фон заголовка (обычно светло-серый)
- `color.table.default.header.text.default` — текст заголовка (темный)
- `color.table.default.header.border.default` — граница заголовка

**Цвета для строк:**
- `color.table.default.row.background.default` — фон обычной строки (белый или чередующийся)
- `color.table.default.row.background.hover` — фон строки при наведении (светло-серый)
- `color.table.default.row.background.selected` — фон выбранной строки (брендовый цвет)
- `color.table.default.row.text.default` — текст в строке
- `color.table.default.row.text.selected` — текст в выбранной строке (обычно белый)
- `color.table.default.row.border.default` — граница строки

**Цвета для ячеек:**
- `color.table.default.cell.text.default` — текст в ячейке
- `color.table.default.cell.border.default` — граница ячейки

**Отступы:**
- `spacing.table.default.cell.padding.x` — горизонтальный отступ в ячейке
- `spacing.table.default.cell.padding.y` — вертикальный отступ в ячейке
- `spacing.table.default.row.gap` — расстояние между строками (если используется)

**Типографика:**
- `typography.table.default.header.font_size` — размер шрифта заголовка
- `typography.table.default.header.font_weight` — вес шрифта заголовка (обычно semibold или bold)
- `typography.table.default.body.font_size` — размер шрифта тела таблицы
- `typography.table.default.body.font_weight` — вес шрифта тела таблицы

**Остальное:**
- `border_radius.table.default` — скругление углов таблицы (если нужно)

#### Что буду делать, если токена нет?

Если мне нужен токен, которого еще нет:

1. **Обращаюсь к дизайнеру дизайн-системы** — я иду к дизайнеру ДС и объясняю, какой токен нужен и где он будет использоваться
2. **Дизайнер ДС создает токен** — дизайнер ДС создает новый токен (например, `color.table.default.header.icon.sorted` для сортировки столбца, где `icon` — это subcomponent внутри header), проверяет консистентность, согласовывает с лидом и фронтенд-разработчиками

**Пример:** Если мне нужен токен для состояния "loading" (загрузка данных в таблице), я обращаюсь к дизайнеру ДС и объясняю необходимость. Дизайнер ДС создаст токены `color.table.default.row.background.loading` или `color.table.default.skeleton.background`, согласует с лидом и фронтенд-разработчиками.

---

## Пример 4: Бейдж (Badge)

### Я как дизайнер хочу создать компонент "badge"

#### Что этот компонент из себя представляет?

Бейдж — это небольшая метка или тег, который показывает статус, категорию или другую краткую информацию. Бейдж должен быть компактным, заметным и легко читаемым.

#### Какой он бывает? Какие у него варианты?

Бейдж имеет варианты (variant) по типу информации:
- **Success** — успешное состояние (зеленый)
- **Error** — ошибка (красный)
- **Warning** — предупреждение (желтый/оранжевый)
- **Info** — информация (синий)
- **Neutral** — нейтральный (серый)

У бейджа обычно нет состояний (state), так как он статичен, но могут быть размеры:
- **Small** — маленький бейдж
- **Medium** — средний бейдж
- **Large** — крупный бейдж

#### Какие элементы у бейджа?

Бейдж состоит из:
- **Background** — фон бейджа
- **Text** — текст бейджа
- **Border** — граница (опционально)
- **Padding** — внутренние отступы
- **Border radius** — скругление (обычно полное, pill-форма)

#### Какие я выберу токены?

Для бейджа я создам следующие токены (success, error, warning, info, neutral — это варианты):

**Цвета для каждого варианта:**
- `color.badge.success.background.default` — фон success бейджа (светло-зеленый)
- `color.badge.success.text.default` — текст success бейджа (темно-зеленый)
- `color.badge.success.border.default` — граница success бейджа (опционально)

- `color.badge.error.background.default` — фон error бейджа (светло-красный)
- `color.badge.error.text.default` — текст error бейджа (темно-красный)
- `color.badge.error.border.default` — граница error бейджа

- `color.badge.warning.background.default` — фон warning бейджа (светло-желтый)
- `color.badge.warning.text.default` — текст warning бейджа (темно-желтый)
- `color.badge.warning.border.default` — граница warning бейджа

- `color.badge.info.background.default` — фон info бейджа (светло-синий)
- `color.badge.info.text.default` — текст info бейджа (темно-синий)
- `color.badge.info.border.default` — граница info бейджа

- `color.badge.neutral.background.default` — фон neutral бейджа (светло-серый)
- `color.badge.neutral.text.default` — текст neutral бейджа (темно-серый)
- `color.badge.neutral.border.default` — граница neutral бейджа

**Отступы:**
- `spacing.badge.default.padding.x` — горизонтальный отступ
- `spacing.badge.default.padding.y` — вертикальный отступ

**Размеры:**
- `size.badge.default.small.height` — высота маленького бейджа
- `size.badge.default.medium.height` — высота среднего бейджа
- `size.badge.default.large.height` — высота большого бейджа

**Типографика:**
- `typography.badge.default.font_size.small` — размер шрифта для маленького бейджа
- `typography.badge.default.font_size.medium` — размер шрифта для среднего бейджа
- `typography.badge.default.font_size.large` — размер шрифта для большого бейджа
- `typography.badge.default.font_weight` — вес шрифта (обычно medium)

**Остальное:**
- `border_radius.badge.default` — скругление (обычно full/pill для округлой формы)

#### Что буду делать, если токена нет?

Если мне нужен токен для нового варианта бейджа:

1. **Обращаюсь к дизайнеру дизайн-системы** — я иду к дизайнеру ДС и объясняю, какой вариант нужен и почему
2. **Дизайнер ДС создает токены** — дизайнер ДС создает все необходимые токены для нового варианта (например, `color.badge.primary.background.default`, `color.badge.primary.text.default` и т.д.), проверяет консистентность, согласовывает с лидом и фронтенд-разработчиками

**Пример:** Если мне нужен вариант бейджа для статуса "pending" (ожидание), я обращаюсь к дизайнеру ДС и объясняю необходимость. Дизайнер ДС создаст токены `color.badge.pending.background.default` и `color.badge.pending.text.default`, согласует с лидом и фронтенд-разработчиками.

---

## Общие принципы работы с токенами

### Когда обращаться к дизайнеру ДС за новым токеном?

Обращайся к дизайнеру дизайн-системы, если:
- Элемент имеет уникальное визуальное представление, которое не может быть выражено существующими токенами
- Элемент имеет состояние, которое требует отдельного токена
- Элемент является частью компонента и должен быть переиспользуемым

### Когда НЕ нужно обращаться за новым токеном?

Не обращайся за новым токеном, если:
- Токен уже существует в системе для нужного элемента компонента
- Токен будет использоваться только один раз и не является частью системы (в этом случае можно использовать значение напрямую, без создания токена)

### Процесс работы с дизайнером ДС

1. **Определи, какой токен нужен** — подумай о структуре и названии токена, который тебе нужен для конкретного элемента компонента
2. **Обратись к дизайнеру ДС** — объясни, какой токен нужен, почему он нужен и где будет использоваться
3. **Дизайнер ДС создает токен** — дизайнер ДС создает токен, проверяет на конфликты, согласовывает с лидом дизайн-системы и фронтенд-разработчиками, добавляет в систему и документирует

### Работа с дизайнером дизайн-системы

Дизайнер дизайн-системы:
- Создает новые токены по твоим запросам
- Проверяет консистентность токенов
- Согласовывает токены с лидом дизайн-системы
- Согласовывает токены с фронтенд-разработчиками
- Документирует решения
- Добавляет токены в систему

Всегда обращайся к дизайнеру ДС, если нужен новый токен — не создавай токены самостоятельно.

### Работа с лидом дизайн-системы

Лид дизайн-системы:
- Утверждает структуру новых токенов
- Принимает решения о стратегии токенизации
- Обеспечивает консистентность системы
- Разрешает спорные вопросы

Лид работает с дизайнером ДС, который создает токены. Ты можешь обращаться к лиду напрямую только в особых случаях или для консультаций по стратегии.

### Работа с фронтенд-разработчиками

Фронтенд-разработчики:
- Проверяют техническую реализуемость токенов
- Убеждаются, что токены не конфликтуют с существующим кодом
- Предлагают оптимизации структуры токенов

Согласование с фронтенд-разработчиками происходит через дизайнера ДС, который создает токены. Ты можешь обращаться к фронтенд-разработчикам напрямую для консультаций, но создание токенов — это задача дизайнера ДС.

---

## Заключение

Компонентный подход к токенам позволяет создавать четкую, понятную и масштабируемую систему токенов. Каждый токен привязан к конкретному компоненту, что делает систему интуитивной для дизайнеров.

Помни:
- Структура токена: `[Category].[Component].[Variant].[Subcomponent].[Item].[State]`
- **Variant** — это вариант компонента (filled, outline, success, error)
- **Subcomponent** — это компонент внутри большого составного компонента (header в таблице, icon в кнопке)
- Для простых компонентов без subcomponent структура может быть упрощена
- Всегда проверяй существующие токены перед созданием новых
- Согласовывай новые токены с лидом дизайн-системы и фронтенд-разработчиками
- Документируй решения и объясняй, почему нужен новый токен

Удачи в работе с токенами! 🎨

